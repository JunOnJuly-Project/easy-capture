# 런타임 의존성 (개발 설치는: pip install -e .[dev])
# 비디오 추적(SAM2)은 GPU 전제(ADR 0007). CPU 는 이미지 모드 위주.
PySide6
torch
torchvision
transformers>=4.55
scenedetect[opencv]
av
imageio
imageio-ffmpeg
pillow
numpy
opencv-python
